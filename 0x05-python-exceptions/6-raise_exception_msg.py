#!/usr/bin/python3
def raise_exception_msg(message=""):
    """raises a name exception with a message
    Args:
        message (string): the message to be raised with the name exception
    """
    raise NameError(message)
